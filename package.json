{
  "name": "astro-console",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "effect": "4.0.0-beta.103"
  },
  "optionalDependencies": {
    "@esbuild/darwin-arm64": "0.28.1"
  },
  "workspaces": [
    "apps/server",
    "apps/web",
    "packages/protocol"
  ],
  "scripts": {
    "build": "npm run build --workspace @astro-console/protocol && npm run build --workspace @astro-console/server && npm run build --workspace @astro-console/web",
    "check": "npm run check --workspace @astro-console/protocol && npm run check --workspace @astro-console/server && npm run check --workspace @astro-console/web",
    "test": "npm run build --workspace @astro-console/protocol && npm run test --workspace @astro-console/protocol && npm run test --workspace @astro-console/server && npm run test --workspace @astro-console/web",
    "setup": "npm run build --workspace @astro-console/protocol",
    "dev:inspect": "npm run setup && npm run dev:inspect --workspace @astro-console/server --"
  }
}
