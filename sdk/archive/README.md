# Archive

This directory holds exploratory and transitional code that informed the SDK but
is not part of the active supported surface anymore.

- `archive/ts/`: ad hoc TypeScript entrypoints and one-off debugging scripts
- `archive/py/`: prototype image-processing scripts and comparison tools

These files are kept for reference while the production pipeline settles. They
may require path fixes or environment tweaks if run directly.
