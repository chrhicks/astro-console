export type R2PublisherConfig = {
  readonly accountId: string
  readonly bucket: string
  readonly endpoint: string
  readonly credentialsPath: string
  readonly databasePath: string
  readonly outputsRoot: string
}
